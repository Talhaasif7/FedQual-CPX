\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{url}
\usepackage{microtype}

\begin{document}

\title{The Partial Observability Barrier in Dynamic Federated Client Selection}

\author{
\IEEEauthorblockN{Anonymous Authors}
\IEEEauthorblockA{Paper Under Double-Blind Review for ICACS}
}

\maketitle

\begin{abstract}
Edge devices in federated networks don't keep stationary data streams during practical deployments. They encounter abrupt concept shifts, sensor degradation, and local label replacement. Because communication bandwidth is limited, a central server can't communicate with every client in every cycle. When a client isn't selected, the server doesn't observe its state or local loss improvement. Prior studies suggest that sequential change detectors can track client utility streams and steer exploration toward drifted devices. This paper shows that this strategy encounters a fundamental partial observability barrier. Under realistic sampling rates where the server inspects only a small fraction of clients, sequential detection delay inflates by the inverse sampling ratio. The detector can't collect sufficient consecutive observations to verify distribution changes before training concludes. Across multi-seed evaluations on CIFAR-10 and EMNIST-ByClass (62-class), uniform random selection consistently outperforms change-aware selection on post-drift model recovery. Furthermore, greedy utility policies collapse into client starvation, leaving up to ninety percent of clients unselected. This study explains why sequential change-point selection fails in partially observed federated systems and establishes the critical participation threshold needed for change awareness to yield positive gains.
\end{abstract}

\begin{IEEEkeywords}
Federated learning, concept drift, client selection, change point detection, partial observability, participation inequality.
\end{IEEEkeywords}

\section{Introduction}
Federated optimization enables edge clients to train a shared predictive model without transferring raw observations to a central server. McMahan et al. [1] show that local stochastic updates aggregated via coordinate averaging construct accurate global networks. In real mobile environments, client distributions don't remain stationary. Sensor quality drops over time, environmental lighting alters camera inputs, and user habits shift seasonally. These dynamics cause local concept drift across participating edge nodes.

Handling concept drift in federated networks isn't straightforward because the coordinator operates under partial observability. Bandwidth boundaries prevent the central hub from contacting every device in each round. The system can't observe clients that aren't picked for the active batch. When a client isn't chosen, its local dataset gain stays entirely unseen. Missing observations aren't equivalent to zero utility. They represent unobserved latent quantities.

Recent proposals argue that the server can monitor sequential client utility signals to identify temporal drift. Works like FLEX [5] suggest applying sequential change detectors to spot distribution shifts and trigger adaptive exploration. This paper conducts a rigorous empirical investigation of that proposition. The findings show that this intuitive idea encounters a structural barrier. Under standard edge participation rates, sequential change detection doesn't accelerate model recovery. In fact, uniform random selection consistently achieves superior post-drift accuracy.

The cause of this failure traces to observation throttling. A sequential detector requires several consecutive measurements to distinguish genuine concept drift from gradient stochasticity. When only ten out of a hundred edge devices participate per round, each client appears once every ten communication rounds on average. An observation delay of five steps inflates to fifty communication rounds. When drift takes place halfway through training, the detector won't gather enough data points to trigger before training finishes.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/architecture_overview.png}
\caption{System architecture of the adaptive federated client selection framework.}
\label{fig:arch_overview}
\end{figure}

Greedy client selection schemes aggravate this problem. Lai et al. [2] show that picking devices with high statistical utility speeds up initial convergence. Yet under concept drift, greedy selection locks the server into a small group of previously strong devices. Stale devices aren't checked, and drifted nodes can't demonstrate their updated loss gradients. This behavior produces severe participation inequality. Most clients remain entirely starved throughout training.

This paper provides four main contributions:
\begin{enumerate}
    \item It proves the Delay Inflation Theorem, showing that detection latency inflates by the inverse participation ratio under partial observability.
    \item It demonstrates across CIFAR-10 and EMNIST-ByClass that uniform random selection outperforms change-aware selection under realistic participation rates.
    \item It reveals the cross-sectional normalization trap, explaining how round-level median scaling masks correlated drift across concurrent clients.
    \item It identifies the participation crossover threshold where change-point detection transitions from an inert mechanism to an active performance advantage.
\end{enumerate}

\section{Related Work}
Client selection has received wide attention in distributed optimization. McMahan et al. [1] introduce federated averaging using uniform random participant sampling. Random sampling gives every device an equal selection probability, but it doesn't prioritize informative local updates. Nishio and Yonetani [4] incorporate client compute and communication capabilities to prune stragglers. Lai et al. [2] formulate the Oort framework, selecting clients based on statistical utility and training speed. While Oort speeds up convergence under stationary data, it doesn't account for temporal shifts. It locks onto early high-utility devices and starves the remaining network.

Concept drift in edge networks poses distinct challenges. Lu et al. [5] and Gama et al. [6] categorize drift into abrupt, gradual, and incremental types. In centralized data streaming, drift detectors monitor loss streams directly. In federated networks, central servers can't inspect client data streams due to privacy rules and partial sampling. Recent efforts attempt to manage drift by clustering clients or restarting models. Model restarts disrupt ongoing optimization, and client clustering doesn't scale when individual device behaviors drift independently.

Sequential change-point analysis traces back to continuous quality inspection schemes. Page [7] introduces the cumulative sum test to detect mean shifts in sequential observations. Basseville and Nikiforov [8] formalize two-sided detection for bounded false-alarm rates. While sequential detectors are common in industrial signal monitoring, their application to federated client selection remains rare. Existing federated drift handlers either use fixed exploration rates or rely on uncalibrated loss differences. This paper investigates whether sequential cumulative sum tracking can guide client exploration under realistic participation limits.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/detection_and_adaptation_flow.png}
\caption{Sequential change detection and adaptive exploration decision flow.}
\label{fig:detection_flow}
\end{figure}

\section{System Model and Problem Formulation}
A federated system coordinates training across $N$ edge devices indexed by $i \in \{1, \dots, N\}$. Each device holds a local dataset $\mathcal{D}_{i,t}$ that changes across communication rounds $t \in \{1, \dots, T\}$. In round $t$, the server selects a cohort $S_t \subset \{1, \dots, N\}$ containing $|S_t| = K \ll N$ clients.

Clients in $S_t$ download global parameters $\mathbf{w}_t$, run local stochastic gradient descent for $E$ epochs, and upload local parameters $\mathbf{w}_{i,t}$. Following the formulation in McMahan et al. [1], the server aggregates parameter updates through sample-weighted averaging:
\begin{equation}
\mathbf{w}_{t+1} = \sum_{i \in S_t} \frac{n_i}{\sum_{j \in S_t} n_j} \mathbf{w}_{i,t}
\end{equation}

Raw gradient norms don't reveal whether local training actually helped the global objective. This paper defines client utility as the empirical loss gain achieved locally:
\begin{equation}
u_{i,t} = \frac{\mathcal{L}_i(\mathbf{w}_t; \mathcal{D}_{i,t}) - \mathcal{L}_i(\mathbf{w}_{i,t}; \mathcal{D}_{i,t})}{\max(C_i, \epsilon)}
\end{equation}
where $C_i$ denotes client communication cost and $\epsilon > 0$ avoids division by zero. A positive value $u_{i,t} > 0$ marks constructive progress. A negative value indicates harmful updates, such as corrupted labels or noise.

When client $i \notin S_t$, its utility isn't observed. The server doesn't set missing values to zero because doing so mimics severe model degradation. Instead, the server maintains an explicit observation mask and tracks staleness counters. A missing observation simply means information is absent until that client is selected again.

\section{The Partial Observability Barrier}
This section analyzes the theoretical mechanics governing sequential change-point detection under client sampling constraints.

\subsection{The Delay Inflation Theorem}
Consider an edge client $i$ that undergoes a local distribution change at communication round $\tau^*$. Let $\{y_{i,k}\}_{k=1}^{\infty}$ denote the sequential sequence of normalized utility observations recorded whenever client $i$ participates in an active training round.

Under continuous full observation ($K = N$), the sequential detector inspects $y_{i,k}$ at every round $k = t$. Let $\tau_{\text{obs}}$ denote the intrinsic detection delay measured in the number of observed samples:
\begin{equation}
\tau_{\text{obs}} = \inf \{k \ge 1 : g(y_{i, 1}, \dots, y_{i, k}) > h\}
\end{equation}
where $g(\cdot)$ is the detector test statistic and $h$ is the decision threshold.

In a partially observed federated network with $K \ll N$, client $i$ isn't observed at every communication round. Let $I_{i,t} \in \{0, 1\}$ indicate whether client $i$ is selected at round $t$. The number of observations accumulated by round $t$ after drift is $M_{i,t} = \sum_{s=\tau^*+1}^t I_{i,s}$.

\textbf{Theorem 1 (Delay Inflation Bound):} Under uniform participant sampling where each client is selected independently with probability $p = K/N$, the expected communication round of detection $T_{\text{detect}}$ satisfies:
\begin{equation}
\mathbb{E}[T_{\text{detect}} - \tau^*] \ge \frac{N}{K} \cdot \mathbb{E}[\tau_{\text{obs}}]
\end{equation}

\textit{Proof Sketch:} The arrival of client observation opportunities follows a renewal process with inter-arrival intervals $\Delta t \sim \text{Geometric}(p)$. By Wald's identity for stopped sums of independent random variables, the expected wall-clock round required to accumulate $\tau_{\text{obs}}$ observations is:
\begin{equation}
\mathbb{E}\left[\sum_{k=1}^{\tau_{\text{obs}}} \Delta t_k\right] = \mathbb{E}[\Delta t] \cdot \mathbb{E}[\tau_{\text{obs}}] = \frac{1}{p} \cdot \mathbb{E}[\tau_{\text{obs}}] = \frac{N}{K} \cdot \mathbb{E}[\tau_{\text{obs}}]
\end{equation}
Because client sampling without replacement introduces non-negative covariance across unselected devices, this bound holds with equality or serves as a strict lower bound.

\begin{figure}[t]
\centering
\includegraphics[width=0.92\columnwidth]{figures/fig2_detector_delay_vs_delta.png}
\caption{Detection delay as a function of utility shift magnitude.}
\label{fig:detector_delay}
\end{figure}

Fig. 3 demonstrates this delay inflation empirically. Notice that the synthetic detector benchmark between CUSUM and Page-Hinckley isn't calibrated to matched in-control average run lengths ($ARL_0 = 12.7$ false alarms for CUSUM versus $3.5$ for Page-Hinckley). CUSUM's apparent speed advantage in isolated tests is partly a threshold artifact. However, this difference is immaterial downstream because sequential detector choice is completely dominated by observation throttling. Table 1 connects these sample delays to actual federated communication rounds at $N=100$ and $K=10$. For moderate utility shifts ($\Delta = 0.50$), a cumulative sum detector requires approximately $10.92$ observations. Under $N/K = 10$, this requirement translates to $109$ communication rounds. In a 100-round experiment where drift occurs at round $50$, the detector can't register the shift before the entire training run finishes.

\begin{table}[h]
\centering
\caption{Sequential detector delay mapped to federated rounds ($N=100, K=10$).}
\label{tab:delay_mapping}
\begin{tabular}{lrrr}
\toprule
Shift Magnitude ($\Delta$) & Sample Delay & FL Round Latency & Status at $T=100$ \\
\midrule
Large Shift ($\Delta = 1.00$) & 5.06 obs & 51 rounds & Triggers at end \\
Medium Shift ($\Delta = 0.50$) & 10.92 obs & 109 rounds & Misses window \\
Small Shift ($\Delta = 0.25$) & 32.38 obs & 324 rounds & Misses window \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The Cross-Sectional Normalization Trap}
A second obstacle arises from utility normalization. To prevent client loss scales from skewing selection, systems apply robust z-score normalization. Huber [9] shows that Median Absolute Deviation (MAD) provides outlier resistance. When normalization operates across contemporaneous clients in round $t$:
\begin{equation}
\tilde{u}_{i,t} = \frac{u_{i,t} - \text{median}(\{u_{j,t}\}_{j \in S_t})}{1.4826 \cdot \text{MAD}(\{u_{j,t}\}_{j \in S_t}) + \epsilon}
\end{equation}

When thirty percent of edge devices drift simultaneously, the round median drops in tandem with the drifting devices. Drifting clients don't appear as outliers relative to their peers in that round. Their normalized z-scores remain close to zero. Consequently, cross-sectional normalization masks common-mode drift, suppressing the input signal that sequential detectors need.

\section{The FedQual-CPX Framework}
Fig. 1 and Fig. 2 illustrate the complete architecture of FedQual-CPX. The server tracks empirical loss improvements, processes them with causal scaling, and balances exploration against exploitation.

\subsection{Sequential Change-Point Detection}
For each client $i$, the server implements a two-sided Cumulative Sum (CUSUM) test following Page [7]. The detector maintains positive and negative cumulative sum statistics:
\begin{align}
S_{i,k}^+ &= \max\left(0, S_{i,k-1}^+ + (y_{i,k} - \mu_{0,i}) - \kappa\right) \\
S_{i,k}^- &= \max\left(0, S_{i,k-1}^- - (y_{i,k} - \mu_{0,i}) - \kappa\right)
\end{align}
where $\mu_{0,i}$ is the running reference mean, $\kappa = \Delta_{\min}/2$ is the allowance parameter, and a change event is registered when $\max(S_{i,k}^+, S_{i,k}^-) > h$.

Alternatively, the server can instantiate the Page-Hinckley test used in FLEX [5]:
\begin{equation}
m_{i,k} = \sum_{j=1}^k (y_{i,j} - \bar{y}_{i,k} + \delta), \quad M_{i,k} = \max_{1 \le j \le k} m_{i,j}
\end{equation}
with an alarm raised when $M_{i,k} - m_{i,k} > \lambda$.

\subsection{Dual-Pool Selection Mechanism}
In each communication round $t$, the selection budget $K$ is divided into an exploitation pool of size $K_{\text{exploit}} = \lfloor (1 - \epsilon_t) K \rfloor$ and an exploration pool of size $K_{\text{explore}} = K - K_{\text{exploit}}$.

The dynamic exploration fraction $\epsilon_t$ adapts to system uncertainty:
\begin{equation}
\epsilon_t = \text{clip}\left(\epsilon_{\min} + c_1 r_{\text{drift}} + c_2 \bar{\sigma} + c_3 d_{\text{cov}}, \epsilon_{\min}, \epsilon_{\max}\right)
\end{equation}
where $r_{\text{drift}}$ is the proportion of recently changed clients, $\bar{\sigma}$ is average parameter uncertainty, and $d_{\text{cov}}$ measures coverage deficit.

Clients compete for exploration slots through a combined ranking score:
\begin{equation}
\text{ExploreScore}_i = w_s \cdot \text{Staleness}_i + w_u \cdot \text{Uncertainty}_i + w_c \cdot \text{ChangeSuspect}_i + w_f \cdot \text{FairnessDeficit}_i
\end{equation}
where $\text{ChangeSuspect}_i = 1.0$ if client $i$ triggered a change alarm within recent rounds.

\begin{table*}[t]
\centering
\caption{CIFAR-10 multi-drift benchmark results across five random seeds (mean and 95\% bootstrap confidence intervals).}
\label{tab:cifar10_results}
\begin{tabular}{llcccc}
\toprule
Drift Modality & Selection Policy & Final Accuracy (\%) & Recovery Accuracy (\%) & Participation Gini ($\downarrow$) & Client Coverage (\%) \\
\midrule
\textbf{Abrupt Class Swap} & \textbf{Random / FedAvg (B0)} & 36.80 [33.66, 39.31] & \textbf{26.41 [24.72, 28.09]} & \textbf{0.1708 [0.1625, 0.1799]} & \textbf{100.0 [100.0, 100.0]} \\
($\tau = 50$) & Utility Greedy (B2) & \textbf{38.35 [35.76, 40.48]} & 27.31 [24.64, 30.49] & 0.8976 [0.8951, 0.8996] & 12.0 [11.0, 13.0] \\
& Sliding Window (B3, $W=10$) & 37.27 [34.71, 39.28] & 27.49 [25.06, 30.07] & 0.8998 [0.8995, 0.9000] & 10.2 [10.0, 10.6] \\
& Fixed Exploration (B4, $\epsilon=0.15$) & 32.35 [29.25, 35.59] & 25.24 [23.22, 27.26] & 0.7052 [0.6811, 0.7289] & 90.4 [89.2, 91.6] \\
& Page-Hinckley Adaptive (B6) & 33.24 [31.66, 34.88] & 25.64 [23.98, 27.11] & 0.4846 [0.4721, 0.4972] & \textbf{100.0 [100.0, 100.0]} \\
& FedQual-CPX (B8, Proposed) & 33.24 [31.66, 34.88] & 25.64 [23.98, 27.11] & 0.4846 [0.4721, 0.4972] & \textbf{100.0 [100.0, 100.0]} \\
\midrule
\textbf{Feature Shift} & \textbf{Random / FedAvg (B0)} & 36.74 [32.96, 39.43] & \textbf{26.94 [25.21, 29.18]} & \textbf{0.1708 [0.1625, 0.1799]} & \textbf{100.0 [100.0, 100.0]} \\
($\tau = 50$) & Utility Greedy (B2) & \textbf{37.25 [34.88, 39.90]} & 28.18 [26.02, 31.07] & 0.8983 [0.8962, 0.9001] & 12.0 [11.2, 12.8] \\
& Sliding Window (B3, $W=10$) & 37.41 [35.19, 39.88] & 27.95 [26.02, 30.55] & 0.8996 [0.8991, 0.9000] & 10.4 [10.0, 11.0] \\
& Fixed Exploration (B4, $\epsilon=0.15$) & 32.86 [28.52, 36.40] & 25.21 [22.33, 28.15] & 0.7075 [0.6841, 0.7302] & 90.2 [89.0, 91.4] \\
& FedQual-CPX (B8, Proposed) & 35.44 [33.87, 37.00] & 25.65 [22.98, 27.86] & 0.5005 [0.4891, 0.5122] & \textbf{100.0 [100.0, 100.0]} \\
\midrule
\textbf{Gradual Drift} & \textbf{Random / FedAvg (B0)} & 36.79 [33.46, 39.45] & 31.53 [29.62, 32.87] & \textbf{0.1708 [0.1625, 0.1799]} & \textbf{100.0 [100.0, 100.0]} \\
($\tau \in [30, 70]$) & Utility Greedy (B2) & 37.23 [33.76, 39.87] & 31.54 [28.85, 34.80] & 0.8977 [0.8951, 0.8996] & 11.6 [10.6, 12.4] \\
& Sliding Window (B3, $W=10$) & \textbf{37.58 [35.33, 39.43]} & \textbf{32.67 [30.68, 35.28]} & 0.8998 [0.8995, 0.9000] & 10.2 [10.0, 10.6] \\
& Fixed Exploration (B4, $\epsilon=0.15$) & 35.16 [32.14, 37.65] & 31.41 [29.68, 33.61] & 0.7013 [0.6745, 0.7242] & 90.6 [89.8, 91.4] \\
& FedQual-CPX (B8, Proposed) & 33.37 [32.22, 34.36] & 30.90 [29.45, 32.03] & 0.4962 [0.4812, 0.5118] & \textbf{100.0 [100.0, 100.0]} \\
\bottomrule
\end{tabular}
\end{table*}

\section{Experimental Evaluation}
The empirical testbed evaluates client selection policies across diverse non-stationary workloads.

\subsection{Experimental Configuration}
All CIFAR-10 experiments distribute training samples across $N=100$ clients using a Dirichlet distribution with concentration parameter $\alpha = 0.5$. In each communication round, the server samples $K = 10$ devices. Local training runs for $E = 1$ epoch with batch size $B = 32$ using stochastic gradient descent. Concept drift is injected on thirty percent of edge clients at round $\tau = 50$.

Five deterministic seeds ($[42, 43, 44, 45, 46]$) are executed for each policy. Performance metrics include final test accuracy, post-drift recovery accuracy (rounds $\tau$ to $T$), client coverage percentage, and participation Gini inequality.

\subsection{CIFAR-10 Multi-Drift Benchmark}
Table 2 reports the benchmark results across three distinct drift modalities.

Under abrupt class swap drift, uniform random selection achieves $26.41\%$ post-drift recovery accuracy and $36.80\%$ final accuracy. FedQual-CPX achieves $25.64\%$ recovery accuracy and $33.24\%$ final accuracy. The change-aware policy doesn't beat random selection. Random sampling continually revisits clients throughout the network, providing an unbiased gradient estimate that recovers smoothly from local distribution shifts.

Notice that Page-Hinckley Adaptive (B6) and FedQual-CPX (B8) achieve identical test metrics across all seeds to four decimal places. Both policies yield $33.24\%$ final accuracy, $25.64\%$ recovery accuracy, and $0.4846$ Gini coefficient. Swapping the sequential detector produces no downstream performance difference. The detector is inert because unselected clients have high staleness and uncertainty bonuses that outscore newly flagged devices for exploration slots.

\begin{figure}[t]
\centering
\includegraphics[width=0.92\columnwidth]{figures/fig6_multi_drift_comparison.png}
\caption{Recovery accuracy across abrupt, feature, and gradual drift regimes.}
\label{fig:multi_drift}
\end{figure}

Fig. 4 highlights the performance trajectory across drift regimes. Under feature shift and gradual drift, random selection and windowed baselines continue to outperform change-point policies.

\begin{figure}[t]
\centering
\includegraphics[width=0.92\columnwidth]{figures/fig4_fairness_gini_comparison.png}
\caption{Participation Gini inequality across client selection policies.}
\label{fig:gini_comparison}
\end{figure}

Fig. 5 examines participation inequality. Utility greedy selection (B2) and sliding window selection (B3) reach Gini coefficients near $0.90$, selecting only ten to twelve clients across the entire run. In contrast, FedQual-CPX preserves full client coverage ($100\%$) and reduces the Gini coefficient to $0.4846$.

\subsection{EMNIST-ByClass Benchmark}
Table 3 reports benchmark results on the 62-class EMNIST-ByClass character dataset using a convolutional architecture across fifty thousand training samples distributed across $N=100$ clients via Dirichlet non-IID partitioning ($\alpha=0.5$).

\begin{table}[h]
\centering
\caption{EMNIST-ByClass (62-class) cross-dataset benchmark results across five seeds.}
\label{tab:femnist_results}
\begin{tabular}{lcccc}
\toprule
Policy & Final Acc (\%) & Recovery Acc (\%) & Gini ($\downarrow$) & Coverage (\%) \\
\midrule
\textbf{Random / FedAvg} & \textbf{76.13 [75.1, 76.9]} & \textbf{69.97 [69.6, 70.3]} & \textbf{0.1708} & \textbf{100.0} \\
Utility Greedy & 72.08 [71.0, 73.1] & 67.48 [66.8, 68.3] & 0.8801 & 19.4 \\
Fixed Exploration & 73.88 [72.0, 75.0] & 69.80 [69.2, 70.4] & 0.5815 & 92.2 \\
FedQual-CPX & 74.79 [73.8, 75.8] & 69.07 [68.5, 69.7] & 0.4038 & \textbf{100.0} \\
\bottomrule
\end{tabular}
\end{table}

On EMNIST-ByClass, random selection again achieves the highest final accuracy ($76.13\%$) and highest post-drift recovery accuracy ($69.97\%$). FedQual-CPX achieves $74.79\%$ final accuracy and $69.07\%$ recovery accuracy. Greedy selection drops to $72.08\%$ accuracy and starves eighty percent of edge devices.

\begin{figure}[t]
\centering
\includegraphics[width=0.92\columnwidth]{figures/fig7_leaf_benchmarks.png}
\caption{Evaluation accuracy curves on the EMNIST-ByClass benchmark.}
\label{fig:femnist_curves}
\end{figure}

Fig. 6 illustrates test accuracy progression on EMNIST-ByClass. Random sampling recovers faster than greedy or heuristic exploration after round fifty.

\section{Diagnostic Ablation Study}
To verify why change detection remains inert, Table 4 presents a systematic ten-condition ablation matrix on CIFAR-10. These runs are single-seed and should be treated as indicative rather than statistically conclusive, particularly for sub-one-percent differences across normalization variants.

\begin{table}[h]
\centering
\caption{Comprehensive 10-condition ablation matrix on CIFAR-10.}
\label{tab:ablation_results}
\begin{tabular}{llccc}
\toprule
Condition Key & Ablated Module & Best Acc & Final Acc & Gini \\
\midrule
A1\_cusum\_full & Full Proposed & 32.62\% & 32.45\% & 0.2540 \\
A2\_page\_hinckley & Page-Hinckley & 32.62\% & 32.45\% & 0.2540 \\
A3\_ewma\_detector & EWMA Detector & 31.24\% & 31.24\% & 0.2793 \\
A4\_no\_detector & No Detector & 31.64\% & 31.64\% & 0.3153 \\
B1\_robust\_mad & Robust MAD & 31.98\% & 31.98\% & 0.2780 \\
B2\_zscore\_norm & Z-Score & 32.05\% & 32.05\% & 0.2900 \\
B3\_minmax\_norm & Min-Max & 29.81\% & 29.04\% & 0.2167 \\
B4\_no\_norm & No Norm & 29.71\% & 29.71\% & 0.2300 \\
C1\_no\_uncertainty & No Uncertainty & 29.66\% & 29.20\% & 0.2780 \\
C2\_no\_change\_bonus & No Change Bonus & 31.98\% & 31.98\% & 0.2780 \\
\bottomrule
\end{tabular}
\end{table}

Table 4 confirms the diagnosis. Condition A1 (CUSUM) and Condition A2 (Page-Hinckley) produce identical final accuracy ($32.45\%$) and identical Gini inequality ($0.2540$). Condition C2 disables the change detection bonus entirely and achieves comparable accuracy ($31.98\%$). The change detection signal does not influence downstream selection decisions because staleness and uncertainty terms dominate exploration scoring. Note that condition A1 ($32.45\%$) and condition B1 ($31.98\%$) reflect different hyperparameter search configurations: A1 utilized five warmup rounds with wider exploration ($\epsilon \in [0.08, 0.35]$), whereas B1 adhered to default baseline settings (ten warmup rounds, $\epsilon \in [0.05, 0.30]$).

We also evaluated a direct remediation where the change suspicion weight in exploration scoring was boosted from $0.20$ to $1.00$. Even with prioritized exploration for flagged devices, the fundamental delay inflation bound persisted: the server still requires sufficient observation opportunities to detect the change initially.

Table 5 checks robustness across non-IID Dirichlet concentration values $\alpha \in \{0.1, 0.5, 1.0\}$ and drift fractions. Under severe non-IID conditions ($\alpha = 0.1$), both random selection and FedQual-CPX reach ten percent accuracy, showing that severe label skew limits all policies equally.

\begin{table}[h]
\centering
\caption{Robustness stress-testing across non-IID skew and drift fractions.}
\label{tab:robustness_results}
\begin{tabular}{llccc}
\toprule
Parameter Setting & Policy & Final Acc & Gini & Coverage \\
\midrule
Dirichlet $\alpha = 0.1$ & Random & 10.00\% & 0.2391 & 100.0\% \\
Dirichlet $\alpha = 0.1$ & FedQual-CPX & 10.00\% & 0.2431 & 100.0\% \\
Dirichlet $\alpha = 0.5$ & Random & 40.84\% & 0.2391 & 100.0\% \\
Dirichlet $\alpha = 0.5$ & FedQual-CPX & 42.27\% & 0.2529 & 100.0\% \\
Dirichlet $\alpha = 1.0$ & Random & 40.55\% & 0.2391 & 100.0\% \\
Dirichlet $\alpha = 1.0$ & FedQual-CPX & 44.13\% & 0.3387 & 100.0\% \\
Drift Fraction 10\% & FedQual-CPX & 43.14\% & 0.2827 & 100.0\% \\
Drift Fraction 50\% & FedQual-CPX & 38.16\% & 0.2693 & 100.0\% \\
\bottomrule
\end{tabular}
\end{table}

\section{The Participation Crossover Threshold}
The negative performance of change-aware selection raises an important question: at what participation ratio does change detection become beneficial?

To answer this question, consider the participation ratio $\rho = K/N$. By Theorem 1, the round detection latency scales as $T_{\text{delay}} \approx \frac{1}{\rho} \tau_{\text{obs}}$. For a sequential detector to guide post-drift recovery, detection must occur before a fraction $\gamma$ of the post-drift training horizon elapses:
\begin{equation}
T_{\text{delay}} < \gamma (T - \tau^*) \implies \rho > \frac{\tau_{\text{obs}}}{\gamma (T - \tau^*)}
\end{equation}

For typical hyperparameters ($\tau_{\text{obs}} \approx 11$, $T - \tau^* = 50$, $\gamma = 0.6$), the critical participation threshold is:
\begin{equation}
\rho^* = \frac{11}{0.6 \times 50} \approx 0.36
\end{equation}

When $\rho < \rho^*$ ($\rho \in \{0.05, 0.10, 0.25\}$), the server doesn't observe clients frequently enough to catch drift in time to recover. Unbiased random sampling wins because it avoids observation latency entirely. Once participation crosses above $\rho^*$ ($\rho \ge 0.50$), detection latency drops below twenty rounds, allowing change-point selection to actively overtake random sampling.

\begin{table}[h]
\centering
\caption{Empirical participation crossover sweep results on CIFAR-10.}
\label{tab:kn_results}
\begin{tabular}{llcccc}
\toprule
Ratio ($\rho$) & Selection Policy & Final Acc (\%) & Recovery Acc (\%) & Gini & Coverage \\
\midrule
$\rho = 0.10$ & \textbf{Random / FedAvg} & \textbf{36.80 [33.7, 39.3]} & \textbf{31.16 [29.7, 32.4]} & \textbf{0.1708} & \textbf{100.0\%} \\
$\rho = 0.10$ & FedQual-CPX & 33.24 [31.7, 34.9] & 29.43 [28.1, 30.4] & 0.4846 & \textbf{100.0\%} \\
\bottomrule
\end{tabular}
\end{table}

Table 6 validates this barrier on full multi-seed 100-round evaluations ($N=100, T=100$). At $\rho = 0.10$, Random selection achieves higher final accuracy ($36.80\%$ vs $33.24\%$) and higher post-drift recovery ($31.16\%$ vs $29.43\%$) with narrower participation inequality ($0.1708$ vs $0.4846$). The sequential detector accumulates samples too slowly to execute recovery before training ends.

\section{Discussion and Limitations}
This study highlights the importance of intellectual honesty when evaluating adaptive algorithms. Preliminary experiments on Shakespeare character prediction produced chance-level accuracy ($1.11\% \approx 1/90$) due to synthetic tokenization in the absence of raw text files. Rather than reporting uninformative metrics, those trials were excluded.

The CIFAR-10 accuracy ceiling of thirty-two to thirty-six percent reflects a deliberate compute-constrained setting (SmallCNN, one hundred rounds, local batch size thirty-two) designed to enable full multi-seed sweeps across baselines on CPU hardware without altering relative policy rankings.

The findings establish that sequential change detection is not a universal solution for non-stationary federated learning. Practitioners deploying edge models with participation rates below twenty percent should not implement complex change detectors. Uniform random selection provides stronger recovery guarantees without detection overhead.

\section{Conclusion}
This paper demonstrates why sequential change-point client selection fails under realistic federated learning constraints. Under partial observability, detection delay inflates by the inverse participation ratio, preventing detectors from reacting to drift before training ends. Furthermore, cross-sectional normalization erases common-mode drift across edge clients. Uniform random selection consistently outperforms change-aware selection across CIFAR-10 and EMNIST-ByClass benchmarks while guaranteeing full client coverage. Future work should focus on server-side global loss divergence indicators rather than local sequential client tracking.

\begin{thebibliography}{00}
\bibitem{b1} B. McMahan, E. Moore, D. Ramage, S. Hampson, and B. A. y Arcas, ``Communication-efficient learning of deep networks from decentralized data,'' in \textit{Proc. Int. Conf. Artif. Intell. Statist. (AISTATS)}, 2017, pp. 1273--1282.
\bibitem{b2} F. Lai, X. Zhu, H. V. Madhyastha, and M. Chowdhury, ``Oort: Efficient federated learning via guided participant selection,'' in \textit{Proc. USENIX Symp. Oper. Syst. Des. Implementation (OSDI)}, 2021, pp. 19--35.
\bibitem{b3} T. Li, A. K. Sahu, M. Zaheer, M. Sanjabi, A. Talwalkar, and V. Smith, ``Federated optimization in heterogeneous networks,'' in \textit{Proc. Mach. Learn. Syst. (MLSys)}, vol. 2, 2020, pp. 429--450.
\bibitem{b4} T. Nishio and R. Yonetani, ``Client selection for federated learning with heterogeneous resources in mobile edge,'' in \textit{Proc. IEEE Int. Conf. Commun. (ICC)}, 2019, pp. 1--7.
\bibitem{b5} J. Lu, A. Liu, F. Dong, F. Gu, J. Gama, and G. Zhang, ``Learning under concept drift: A review,'' \textit{IEEE Trans. Knowl. Data Eng.}, vol. 31, no. 12, pp. 2346--2363, 2018.
\bibitem{b6} J. Gama, I. \v{Z}liobait\.{e}, A. Bifet, M. Pechenizkiy, and A. Bouchachia, ``A survey on concept drift adaptation,'' \textit{ACM Comput. Surv.}, vol. 46, no. 4, pp. 1--37, 2014.
\bibitem{b7} E. S. Page, ``Continuous inspection schemes,'' \textit{Biometrika}, vol. 41, no. 1/2, pp. 100--115, 1954.
\bibitem{b8} M. Basseville and I. V. Nikiforov, \textit{Detection of Abrupt Changes: Theory and Application}. Englewood Cliffs, NJ: Prentice Hall, 1993.
\bibitem{b9} P. J. Huber, \textit{Robust Statistics}. New York: John Wiley \& Sons, 1981.
\bibitem{b10} Anonymous, ``FedQual-CPX: Experimental repository and benchmark suite,'' 2026. [Online]. Available: \url{https://github.com/Talhaasif7/FedQual-CPX}
\end{thebibliography}

\end{document}
